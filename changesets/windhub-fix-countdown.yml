- script: windhub
  bump: patch

修复农场模块收割和种植后倒计时不更新的问题。

**问题根源：**
原生网页的状态管理函数（state、doAction、syncBoard 等）都在闭包内，脚本直接调用 API 绕过了原生的 doAction，导致闭包内的 state 没有被更新，网页的定时器使用旧数据，倒计时无法更新。

**修复方案：**
在 API 操作后触发原生网页的"刷新状态"按钮（btn-refresh），让网页重新获取状态并渲染，确保闭包内的 state 被更新，倒计时等功能正常工作。
